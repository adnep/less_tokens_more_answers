# Model / inference
torch>=2.1.0
transformers>=4.51.0
accelerate>=0.30.0          # required by transformers for device_map model loading
datasets>=2.19.0
vllm>=0.8.0                 # batch offline sample generation

# Numerics
numpy>=1.26.0
scipy>=1.12.0
sympy>=1.12                 # LaTeX answer parsing/equivalence in evaluation.voting

# Data / config
pandas>=2.0.0
pyyaml>=6.0

# Visualization / UI
matplotlib>=3.8.0
seaborn>=0.13.0
streamlit>=1.30.0          # dtr_explorer interactive app

# Utilities
tqdm>=4.66.0
